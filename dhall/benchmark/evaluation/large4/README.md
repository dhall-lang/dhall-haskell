This example is taken from github:uwedeportivo/ds-dhall

Run dhall --file generate-example.dhall - note that this requires "as Source" imports, or else it won't work due to out-of-memory exception.

With "as Source" the run takes about 3 seconds.
